import React from "react";
import { Box } from "@chakra-ui/react";

const Footer = React.memo(() => (
  <Box bg="gray.100" py={4} px={6} textAlign="center">
    <p>Footer</p>
  </Box>
));

export default Footer;
